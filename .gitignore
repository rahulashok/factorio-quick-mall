# Factorio
*.zip
*.log

# FactorioTest CLI working directory (downloaded mods, headless saves, config,
# and a symlink back into this repo) — generated by scripts/run_tests.sh.
factorio-test-data-dir/

# Claude Code — machine-local state (the process-tasks skill IS tracked)
.claude/scheduled_tasks.json
.claude/scheduled_tasks.lock
.claude/settings.local.json

# OS
.DS_Store
