# Factorio
*.zip
*.log

# FactorioTest CLI working directory (downloaded mods, headless saves, config,
# and a symlink back into this repo) — generated by scripts/run_tests.sh.
factorio-test-data-dir/

# Claude Code — machine-local state (the process-tasks skill IS tracked)
.claude/scheduled_tasks.json
.claude/scheduled_tasks.lock
.claude/settings.local.json

# Obsidian per-machine editor UI state (docs/ is an Obsidian vault)
docs/.obsidian/workspace.json

# Test results are refreshed every run by scripts/run_tests.sh; the initial copy
# was committed, routine refreshes are left as local working-tree changes.
docs/test-results/latest.md

# OS
.DS_Store
